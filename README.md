## Setup database from docker-compose
The first, prepare `docker` and `docker-compose` as environment to build start database.

Execute command:
```aidl
docker-compose up -d
```


## Start project:
```aidl
./gradlew bootRun
```

// TODO - is coming soon
